标签传播算法主要思想：
    传播概率矩阵T和标签矩阵Y的不断迭代更新
    优点：可用少量的标签样本推测出大量无标签样本的标签值

算法流程：
    初始化样本图（knn近邻选取或者直接构建全连接图）,样本标签矩阵Y（*无标签样本标签全初始化为1*）
    根据样本点的相邻程度计算边的权值Wij（普遍采用rbf内核）
    根据权重图W计算节点j至节点i的传播概率Tij， Tij=wij/sum(W)//按行缩减
    while(循环次数<maxiter):
        Y‘ = T*Y（因为传播概率T和样本标签一直保持不变，故可化简为每次嗲带只需要改变无样本标签概率Yu
        简化后：Yu = Tul*Yll + Tuu*Yuu
    最后选取Yu中每行的最大值对应的索引作为无标签样本的标签